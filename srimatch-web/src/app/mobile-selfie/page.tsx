import MobileSelfiePage from "@/views/MobileSelfiePage";

export const metadata = {
  title: "Mobile Identity Verification | SriMatch",
};

export default function Page() {
  return <MobileSelfiePage />;
}
